# HTML

>#### 1. 什么是HTML

```  
HTML指的是 *超文本标记语言* (Hyper Text Markup Language)
```

>#### 2. 文本格式化标签

| 语义   | 标签                           |
| ------ | ------------------------------ |
| 加粗   | `<strong></strong>`或`<b></b>` |
| 倾斜   | `<em></em>`或`<i></i>`         |
| 删除线 | `<del></del>`或`<s></s>`       |
| 下划线 | `<ins></ins>`或`<u></u>`       |
| 上标   | `<sup></sup>` eg:3<sup>2</sup> |
| 下标   | `<sub></sub>` eg:3<sub>2</sub> |

>#### 3. 超链接

```html
属性target: 默认值为 _self: 在当前窗口打开页面; _blank：新窗口打开页面;
空链接: href的值为 "#" 或者 "javascript:;" ;
锚点链接: 可快速定位到页面中某个位置,锚点链接的href属性中多了一个 "#" 号  
    链接处: <a href="#live">跳转到个人生活</a>
    内容处: <h3 id="live">我是个人生活</h3>
```

```html
<base target="_blank"/>

**base 标签**
  1) 是个单标签;
  2) 必须写在<head>标签中;
  3) 用来统一设置当前网页中所有超链接(<a>标签)的打开方式,是在当前页面打开还是新页面;
  4) 如果<base>标签和<a>标签都设置了target属性,会以<a>标签的设置为准;
```

>#### 4. 表格

```html
<table border="1" align="center" cellpadding="20" cellspacing="0" width="500" height="300">
  <tr>
    <td>1</td>
    <td>2</td>
    <td>3</td>
  </tr>
  <tr>
    <td>1</td>
    <td colspan="2" rowspan="2">2</td>
  </tr>
  <tr>
    <td>1</td>
  </tr>
</table>

  **表格相关属性**
    border: 表格边框,1代表一个px, 如果等于2,代表表格最外层边框粗细为2px;
    align: left|center|right,表格相对于父元素的对齐方式,和表格内容无关哦;
    cellpadding: 单元格内容与边框之间的距离;
    cellspacing: 单元格之间的距离,默认是1;
    colspan: 跨列合并单元格 | 横着合并;
    rowspan: 跨行合并单元格 | 竖着合并;
```

>#### 5. 列表

  ```html
  <!-- 无序列表 -->
  <ul>
    <li>这是内容</li>         <!-- ·这是内容 -->
  </ul>
  <!-- 有序列表 -->
  <ol>
    <li>这是内容</li>         <!-- 1.这是内容 -->
  </ol>
  <!-- 自定义列表 -->
    <dl>                      //表现形式   
      <dt>联系我们</dt>         联系我们
      <dd>微博</dd>               微博
      <dd>公众号</dd>             公众号
    </dl>
  ```

>#### 6. 表单

```html
<!-- 隐藏表单 作用:将不想展示给用户,但又要提交给服务器的数据保存在隐藏域中 -->
<input type="hidden">
<!-- 重置表单的填写，需要有form标签包裹住才会生效 -->  
<input type="reset" value="重置">  
<!-- 下拉表单  selected表示默认选中项-->
<select>
  <option>山东</option>
  <option selected>北京</option>
</select>
```


# CSS

>#### 1. 什么是CSS

```
CSS是 *层叠样式表* (Cascading Style Sheets), CSS也是一种标记语言
```

>#### 2. 行内元素和块级元素

```css
div { display: inline | block | inline-block | none }
常见的行内元素:<span>,<a>,<i><em><b><s>···
常见的块级元素:<h1>~<h6>,<p>,<div>,<table>,<ul><ol><li>,<dl><dt><dd>···
行内块元素:<img>,<input>,<td>··· 
// 行内块元素是行内元素，但是拥有块级元素的属性
```

>#### 3. 背景

```css
div{
  background-color: red | #ff0000 | #f00 | rgb:(255,0,0) | rgba(255,0,0,1);
  background-image: url(图片路径);
  background-repeat: no-repeat | repeat | repeat-x | repeat-y;
  background-position: left | right | top | bottom | center | 像素(不带单位);
  background-attachment: scroll(默认滚动) | fixed(背景固定); 
}
 ※position: 使用方位名词的时候,x y的位置可以改变;使用像素后位置不能改变
```

>#### 4. CSS的三大特性

```
**层叠性、继承性、优先级**

可以被继承的属性: font-字体系列 | text-文本系列 | color | list-style | line-height |  
border-collapse | visibility元素可见性 | cursor光标属性 | z-index堆叠顺序;

<a>标签的字体颜色不能被继承; <h1>~<h6>标签字体大小不能被继承

不能被继承的属性: display属性 | vertical-align设置基线，常用于图文对齐 | 
text-decoration下划线 | 盒子模型的属性: width、height、padding、margin等 | 
background-背景系列 | 定位属性: float、clear、position、transform等 
```

>#### 5. 盒模型

```
1. 普通情况下,设置border和padding 都会撑大盒子;
  以下两种方法可以不撑大盒子:
    1) 设置 box-sizing: border-box; 默认值为 content-box;
    2) 不设置width属性,让width默认为可视区的width,此时设置border+padding都不会撑大盒子宽度;
```

```css
2. 当嵌套关系(父子关系)的块元素都设置了上外边距的时候,此时会合并上外边距,父元素展示较大的那个值;
    示例: 
      div {
        width: 200px; height: 200px; background-color: pink;
        margin-top: 50px;
      }
      p {
        width: 100px; height: 100px; background-color: purple;
        margin-top: 100px;
      }
      <div><p></p></div>
      /* 此时div的上外边距是 100px */

    解决方案有:
    
      1) 为父元素定义上边框 (border-top: 1px solid transparent;)
      2) 为父元素定义上内边距 (padding-top: 1px;)
      3) 为父元素添加 (overflow: hidden;)
      4) 还可以使用 浮动 | 定位 等其他方法;
```


>#### 6. 浮动

```
浮动特性: 
  1) 浮动元素会脱离标准流; 不保留原先的位置;
  2) 浮动元素会在一行内显示并且在元素顶部对齐;
  3) 浮动元素具有 行内块元素 特性;
```

```css
3. 说一下父级块元素塌陷问题的原因, 并给出几种解决方案;

  塌陷的原因: 当父元素不方便设置高度, 子元素又全部设置了浮动的时候,就会出现父元素的高度塌陷问题;
    实例:
      .father{ background-color: pink; }
      .son{
        float: left;
        width: 100px; height: 100px; background-color: purple;
      }
      <div class="father"> 
        <div class="son"></div> 
      </div>

      解决方案:

      1) 额外标签法,也叫隔墙法 
        在最后一个浮动的子元素后面添加一个块级元素 <div class="clear"></div>; 
        为其添加样式 .clear{ clear: both}
        优点: 简单易懂,书写方便;  缺点: 会添加无意义的标签 结构冗余;

      2) 为父元素添加 (overflow: hidden); 优点: 代码简洁; 缺点: 无法显示溢出部分;

      3) 为父元素添加after伪元素
        .clearfix:after { content:''; display: block; height: 0; clear: both; visibility: hidden;}
        .clearfix { *zoom: 1;} /* IE6、7 专有*/
        然后给 父元素添加 clearfix 类名
        优点: 没有增加标签，结构更简单  缺点: 照顾低版本浏览器

      4) 为父元素添加双伪元素
        .clearfix:before, .clearfix:after { content:''; display: table;}
        .clearfix:after { clear: both;}
        .clearfix { *zoom: 1;} /* IE6、7 专有*/
        然后给 父元素添加 clearfix 类名
        优点: 没有增加标签，结构更简单  缺点: 照顾低版本浏览器
```

>#### 7. 定位

```css
position: static | relative | absolute | fixed | sticky

static: 静态定位 无定位

relative: 相对定位 
注意点:  1) 移动时参照自己原来的位置; 
        2) 移动后依旧保留原来的位置;

absolute: 绝对定位 
注意点:  1) 移动时参照有定位的祖先元素,如果都没有定位,则以浏览器为准;
        2) 移动后不保留原来的位置;

绝对定位的盒子怎么设置水平垂直居中?
方法: 
div{
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%,-50%);
}        

fixed: 固定定位
注意点:  1) 移动时以浏览器的可视窗口为准; 和父元素没有关系; 不随滚动条滚动;
        2) 移动后不保留原来的位置;

固定定位的用法: 把盒子固定在版心右侧位置 
方法是:  left:50% 可以移动到版心中间;  margin-left: 版心宽度的一半; 

sticky: 粘性定位
注意点: 1) 移动时以浏览器的可视窗口为准; (和固定定位一致)
       2) 移动后依旧保留原来的位置; (和相对定位一致)
       3) 必须添加 top/bottom/left/right 其中一个才能生效;
```

>#### 8. 常用易忘的CSS样式

```css
1. 经过父盒子,让子盒子展示出来
  .father:hover .son {
    display: block;
  }
2. css三角形的做法
  div{
    width: 0;
    height: 0;
    line-height: 0; font-size: 0; /* 这两句代码是兼容低版本浏览器 */
    border: 10px solid transparent;
    border-left-color: purple;
  }
3. 鼠标样式
  div{ cursor: pointer | move | text | not-allowed}
4. 取消表单选中时的边框轮廓
  div{ outline: 0 | none}
5. 取消textarea文本域表单的拖拽效果
  div{ resize: none;}
6. 图文垂直对齐,只针对行内元素和行内块元素有效
  img{ vertical-align: baseline | middle | top | bottom }
  /* baseline:默认,基线对齐 | middle:中线对齐 | top:顶线对齐 | bottom: 底线对齐 */
  /* 可以解决图片底部的默认小空隙,小空隙产生的原因是因为图文默认是基线对齐,小空隙是留给pjg等字母的; */
7. 文本溢出使用省略号显示
  单行文本:
  div{ 
    white-space: nowrap; /* 强制文本一行展示,不换行 */
    overflow: hidden; /* 隐藏溢出的文字 */
    text-overflow: ellipsis; /* 用省略号代替溢出的文字 */
  }
  多行文本: (有兼容问题)
  div {
    overflow: hidden;
    text-overflow: ellipsis;
    display: -webkit-box; /* 弹性伸缩盒子模型显示*/
    -webkit-line-clamp: 2; /* 限制显示几行文本 */
    -webkit-box-orient: vertical; /* 设置或检索伸缩对象的子元素的排列方式 */
  }
8. margin 负值的妙用
  在浮动的时候,会出现相邻的盒子边框变粗,此时可给每一个盒子增加一个 margin-left:-1px;
  做完后如果需要做鼠标经过盒子边框变色的操作:
    1) 如果盒子没有定位,就在盒子经过时增加一个相对定位;
    2) 如果盒子有定位了,就给盒子加一个 z-index;
9. 文字围绕浮动元素的巧妙布局
  左图右文字的布局,可以给左图加一个左浮动,右边的文字会自动围绕浮动的左图来显示,不需要其他操作;
10. 三角形的做法
  /* 右下直角边三角形 */
  div {
    border-top: 40px solid transparent;
    border-right: 10px solid red;
  }
  /* 右箭头三角形 */
  div {
    border-top: 40px solid transparent;
    border-right: 40px solid transparent;
    border-bottom: 40px solid transparent;
    border-left: 40px solid red;
  }
11. 修改input 里 placeholder 的文字颜色
  input::placeholder{
    color:#ccc;
  }
12. :nth-child() 和 :nth-of-type() 的区别
  先看结构
  nav :nth-child(1) {
    background-color: pink; 
  }
  nav :nth-of-type(1) {
    color: red;
  }
  <nav>
    <p>1</p> 
    <p>2</p>
    <div>3</div> 
    <div>4</div>
    <h4>5</h4> 
    <h4>6</h4>
  </nav>
  分析:
  nav :nth-child(1)   不区分盒子标签,在所有子盒子中选择第一项; 所以只有<p>1</p> 被选中;
  nav :nth-of-type(1) 会区分盒子标签的类型,选出每种类型的第一项; 所以第 1 3 5项 被选中;

  nav div:nth-child(1)    会先选择第一项,再核对标签,第一项不是div标签,所以选不中任何项
  nav div:nth-of-type(1)  先核对标签,再选择该标签的第一项, 会选择 3
  
13. 模糊样式
  filter:blur(5px)  像素越大越模糊

14. 过渡
  div {
    /* transition: 要过渡的属性 花费的时间 [运动曲线 何时开始] */
    transition: width 1s ease 2s; /* width 在2s后开始 在1s内 以逐渐变慢的速度 变化*/
  }

  /* 谁要做过渡给谁加 width */
  div {
    width: 50px; height: 50px; background-color: pink;
    transition: width 1s;
  }
  div:hover { width: 200px;}

```